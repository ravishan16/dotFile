# -----------------------------------------------------------------------------
# Global .gitignore file for macOS Developers using VS Code
#
# Add patterns here that should be ignored in *all* your Git repositories.
# Project-specific ignores should go into the project's own .gitignore file.
# -----------------------------------------------------------------------------

# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
# Icon must end with two \r
Icon?
._*
.Spotlight-V100
.Trashes
ehthumbs.db # Thumbnail cache files
Thumbs.db

# --- IDEs / Editors ---

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets
.history/

# Other common IDEs (optional, uncomment if needed)
# .idea/
# *.sublime-project
# *.sublime-workspace
# .project
# .classpath
# .settings/

# --- Build Artifacts & Logs ---
build/
dist/
out/
bin/
*.o
*.obj
*.exe
*.dll
*.so
*.dylib
*.log
logs/
*.tmp
tmp/

# --- Common Backup/Swap Files ---
*.swp
*~
*.bak
*.orig
*.rej # Patch rejects

# --- Dependency Directories ---
node_modules/
jspm_packages/
bower_components/
vendor/ # Common for PHP/Ruby/Go
Pods/ # CocoaPods

# --- Python Specific ---
*.py[cod] # .pyc, .pyo, .pyd
__pycache__/
*.egg-info/
*.egg
pip-wheel-metadata/
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
# Jupyter Notebook
.ipynb_checkpoints
# Coverage
.coverage
coverage.xml
htmlcov/
# Virtual Environments (also see Conda section)
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# --- Conda Specific ---
# Environment directories (usually project-specific)
conda-env/
# Conda package cache (rarely local to project)
pkgs/
# NOTE: environment.yml or meta.yaml files should usually be committed,
# so they are NOT ignored globally here.

# --- Node.js Specific ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Lockfiles are often committed per-project, ignore globally if preferred
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
# Framework build outputs
.next/ # Next.js
.nuxt/ # Nuxt.js
.svelte-kit/ # SvelteKit
.cache/ # Gatsby, Remix, etc.
public/build/ # Remix
# Other cache/config
.npm/
.yarn/

# --- Web Frontend (CSS/HTML/JS) ---
# CSS Preprocessor Cache
.sass-cache/
.less-cache/
# Minified files (often in build/dist, but can appear elsewhere)
*.min.css
*.min.js
# Styleguide builds
styleguide/

# --- Secrets / Sensitive Data ---
# Ignore all .env files by default, but allow examples
*.env
.env.*
!.env.example
# Credentials and keys
secrets.*
*.pem
*.key
credentials.*
config.*.yml
config/credentials.yml.enc # Rails encrypted credentials

# --- Terraform ---
.terraform/
*.tfstate
*.tfstate.backup
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# --- Archives ---
*.zip
*.tar.gz
*.rar
*.7z

# --- Other ---
*.DS_Store? # Case-insensitive version

# If you use Docker often, you might add common Docker ignore patterns here too
# .dockerignore is usually project-specific though

